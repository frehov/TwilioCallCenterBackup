spring:
  profiles:
    active: @activatedProperties@,menu

management:
  endpoint:
    prometheus:
      enabled: true
  endpoints:
    web:
      exposure:
        include: prometheus, health

management:
  metrics:
    enable:
      jvm: false
      logback: false
      tomcat: false

twilio:
  account:
    sid: xxxxxxxxx
    api:
      secret: xxxxxxxxxx
